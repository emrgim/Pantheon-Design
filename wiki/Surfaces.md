# Surfaces

## dei.mrg.im

Static catalog in `/home/domvs/olimpo`. Photos: `/renders/<id>.jpg`, `/renders/<id>-face.jpg`, `/avatars/`. **Do not regenerate portraits.**

Chrome: plate `#1E5197`, white type, IBM Plex, dashed W4, radius 0, faint code grain on `html,body` only.

Inline icons use `currentColor` (white on the plate). Pack download `color.svg` is `#1E5197` on transparent (for light paper). On the blue site, prefer white stroke.

Build: `build.py` — never call `fetch_render`.

## Google Slides

Template kit (16 layouts):  
https://docs.google.com/presentation/d/1gBhmzcurAdY5NAMxz0RWjj2bbLKhO0WBQr1reJ7Inxc/edit

Plate library (gods + building):  
https://docs.google.com/presentation/d/1hrdBBCmXskEiBUz2dFX8UJn5Is06x2Jgb5xp2Uam9C4/edit

Same tokens. Same English. Same one blue.
